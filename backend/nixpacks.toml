# Force Nixpacks to use Node.js (not Deno)
[phases.setup]
nixPkgs = ["nodejs_22"]
nixOverlays = []
nixLibs = []

[phases.install]
cmds = ["npm ci"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
