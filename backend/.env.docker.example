# Backend env for Docker/Compose
# Copy to backend/.env (for local node runs) or use Compose env vars

# Core
PORT=3001
NODE_ENV=production

# Database (use local Compose postgres service)
DATABASE_URL=postgresql://postgres:postgres@db:5432/aishacrm

# Supabase Auth
SUPABASE_URL=https://ehjlenywplgyiahgxkfj.supabase.co
SUPABASE_ANON_KEY=REPLACE_WITH_YOUR_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_YOUR_SUPABASE_SERVICE_ROLE_KEY

# Security
JWT_SECRET=REPLACE_WITH_A_SECURE_RANDOM_SECRET
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Storage (optional)
SUPABASE_STORAGE_BUCKET=tenant-assets

# Redis/Valkey (for ephemeral AI memory)
# In Compose, services use the docker network name `redis`
REDIS_URL=redis://redis:6379

# System tenant UUID mapping for archival + memory normalization
SYSTEM_TENANT_ID=a11dfb63-4b18-4eb8-872e-747af2e37c46

# Optional integrations (leave blank if unused)
OPENAI_API_KEY=
# n8n Integration
# Base URL of your n8n container inside the compose network
N8N_BASE_URL=http://n8n:5678
# Optional API key if Public API is enabled in n8n
N8N_API_KEY=
# Optional Basic Auth for legacy /rest endpoints (enable in n8n service)
N8N_BASIC_AUTH_USER=
N8N_BASIC_AUTH_PASSWORD=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
