# Force LF line endings for shell scripts (Docker compatibility)
*.sh text eol=lf

# Force LF for entrypoint scripts
*entrypoint.sh text eol=lf

# Force LF for git hooks (husky)
.husky/* text eol=lf

# Auto detect text files and normalize line endings to LF in the repository
