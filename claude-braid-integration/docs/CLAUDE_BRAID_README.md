# Using Claude with Braid

Open this repo in VS Code with Claude. Paste a template from `prompts/`. Generate a single `.braid` file. Run `node tools/braid-check file.braid`. Fix diagnostics and re-run until clean.
