# AiSHA Agent Bootstrap

Load `prompts/claude_system_braid.md`. Convert chat requests into goal+schemas+caps. Generate a Braid file. Run `braid-fmt` and `braid-check`. Build and run under policy if needed.
