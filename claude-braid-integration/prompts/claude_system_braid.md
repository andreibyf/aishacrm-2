# System Instructions for Claude in this repo (Braid-first)

Follow Braid rules. Output only Braid. No nulls; use Option/Result. Declare effects after `!`. Use explicit capabilities. Always run `braid-fmt` then `braid-check` and fix diagnostics.
