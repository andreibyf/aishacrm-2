# Task Prompt Templates

A) API Endpoint: Goal {text}, Input {json}, Output {json}. Produce `.braid` with `fn main(http: Http, clock: Clock) !net,clock`.
B) Data Transform: Goal {text}, Input {json}, Output {json}. Produce `.braid` with `fn main(input: Json) -> Result[Json, Err]`.
