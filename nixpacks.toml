# Force Nixpacks to use Node.js (not Deno)
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s dist -l $PORT"

[variables]
# Increase Node.js heap for Vite build to avoid OOM on 1GB plans
NODE_OPTIONS = "--max-old-space-size=896"
