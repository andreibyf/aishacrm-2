#!/bin/sh
# Run lint to catch syntax errors (allow warnings)
npm run lint

# Run build to catch compile errors
# Use build:ci for local development (Doppler has Windows compatibility issues)
# Production/CI will use Doppler in Docker where it works correctly
npm run build:ci
