#!/bin/sh

echo ""
echo "╔══════════════════════════════════════════════╗"
echo "║           PRE-COMMIT QUALITY GATE            ║"
echo "╚══════════════════════════════════════════════╝"
echo ""

# ── Step 1: ESLint ──────────────────────────────────────────────
echo "🔍 Step 1/3: Running ESLint..."
npm run lint -- --max-warnings 9999
if [ $? -ne 0 ]; then
  echo ""
  echo "❌ ESLint failed — fix errors above before committing"
  exit 1
fi
echo "✅ ESLint passed"
echo ""

# ── Step 2: Braid core tests ───────────────────────────────────
echo "🧪 Step 2/3: Running Braid core tests..."
BRAID_CORE="braid-llm-kit/core"
if [ -d "$BRAID_CORE" ] && ls "$BRAID_CORE"/*.test.js >/dev/null 2>&1; then
  node --test "$BRAID_CORE"/*.test.js
  if [ $? -ne 0 ]; then
    echo ""
    echo "❌ Braid core tests failed — fix errors above before committing"
    exit 1
  fi
  echo "✅ Braid core tests passed"
else
  echo "⏭️  No Braid core test files found — skipping"
fi
echo ""

echo "╔══════════════════════════════════════════════╗"
echo "║        ✅ PRE-COMMIT CHECKS PASSED           ║"
echo "╚══════════════════════════════════════════════╝"
echo ""
