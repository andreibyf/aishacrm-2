- **Prod schema parity with staging — DocuSeal migrations 159, 160, 161 applied to prod (4VD-40) (Supabase project `ehjlenywplgyiahgxkfj`, `docs/reference/DATABASE_REFERENCE.md`)**: Prod was 3 migrations behind staging — `docuseal_submissions` table didn't exist, `tenant.slug` didn't exist. Anything that called `from('docuseal_submissions')` or selected `tenant.slug` on prod would 404 silently. Applied via `apply_migration` MCP against prod, exact bodies copied from `backend/migrations/159_docuseal_integration.sql`, `160_docuseal_supabase_storage_path.sql`, `161_tenant_slug.sql`. **159**: `docuseal_submissions` table (23 cols incl. tenant_id FK to `tenant(id)` ON DELETE CASCADE, polymorphic `(related_to, related_id)` pointer matching activities pattern, status state machine `pending|sent|viewed|signed|completed|declined|expired|failed`, lifecycle timestamps, idempotency fields, jsonb metadata) + 4 RLS policies (select/insert/update/delete via `users.tenant_id` join, service_role bypass for webhook writes) + 3 indexes (entity lookup, status filter, created_at DESC) + auto-`updated_at` trigger via `docuseal_set_updated_at()` function. **160**: `supabase_storage_path TEXT` column on `docuseal_submissions` for the signed-PDF mirror to tenant-assets bucket (closes the durability gap where signed PDFs only lived in DocuSeal's Docker volume on VPS-2). **161**: `tenant.slug TEXT NOT NULL UNIQUE` with format CHECK `^[a-z0-9](-?[a-z0-9])+$` length 2..64. Backfill is deterministic: lowercased name → non-alphanumeric replaced with hyphen → trimmed of leading/trailing hyphens → `tenant-<first-8-of-uuid>` fallback if empty/<2 chars → `-<row_number>` collision suffix ordered by id. All 7 prod tenants backfilled cleanly (`4v-data-consulting`, `client-playgorund`, `dfit-factor`, `g-o-d-assets-llc`, `labor-depot`, `local-development-tenant`, `team-realtor` — the `client-playgorund` typo carries through faithfully from `tenant.name` and is editable in tenant settings). **PostgREST schema reload** issued via `NOTIFY pgrst, 'reload schema'` per migration runbook (memory: `apply_migration` does NOT reliably trigger auto-reload). **Verification SQL** (executed post-apply): table exists, column count = 23, `supabase_storage_path` present, `tenant.slug` present, 5 indexes (PK + 3 explicit + UNIQUE), 4 RLS policies, 7/7 tenants slugged. REST verification from sandbox blocked by egress allowlist (Supabase host not on the cowork network allowlist) — SQL verification suffices since schema/RLS/index existence is the contract; the REST surface gets exercised on next prod backend deploy. **DATABASE_REFERENCE.md updated**: added staging row to environments table (was missing), bumped prod table count 88 → 89, added `slug` to `tenant` columns with the CHECK + backfill rationale, last-updated 2026-04-02 → 2026-05-07. **Closes 4VD-40.**
