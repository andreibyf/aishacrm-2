- **Braid registry policy gate audited end-to-end + integration test pinning `.braid` `@policy` ↔ `registry.js` parity (4VD-38, follow-up to 4VD-10) (`backend/__tests__/braid/registry-policy-integration.test.js`)**: Trace of `TOOL_REGISTRY[name].policy` consumers in `backend/lib/braid/execution.js`: the field gates **(1) role check** via `CRM_POLICIES[policy].required_roles` — `WRITE_OPERATIONS` requires `user|manager|admin|superadmin|agent`, `READ_ONLY` allows ALL roles (`required_roles: []`); **(2) rate limit** — READ_ONLY = 100 req/min, WRITE_OPERATIONS = 50 req/min; **(3) delete confirmation** — `requires_confirmation` flag; **(4) Redis result caching** — `isReadOnly = config.policy === 'READ_ONLY'` flips a 90-second `TOOL_CACHE_TTL.DEFAULT` keyed on `(toolName, tenantUuid, normalizedArgs)`; **(5) audit log policy field** — written unconditionally to `braid_audit_log` regardless of `audit_required` (verified at `metrics.js:158` `logAuditEntry`, called from execution.js:279/397/441 across cache-hit / success / error paths). **Implication of the 4VD-10 misclassification**: the 10 tools downgraded to READ_ONLY had no role gate (any role incl. `employee` could call them), 2× higher rate limit, AND — most concerning — their results were Redis-cached for 90s, so identical-args repeats of `initiate_call`, `send_document_for_signing`, `invite_user`, `clear_report_cache` would have returned cached "Ok" responses WITHOUT actually invoking the side effect. **Audit log review across prod (`ehjlenywplgyiahgxkfj`, 130 rows / 18 distinct tools / Jan-Apr 2026) and staging (`bjedfowimuwbcnruwcdj`, 5 rows / 2 tools / May 6 2026)** — ZERO executions of any of the 10 misclassified tools (`process_inbound_communication`, `analyze_document`, `send_document_for_signing`, `draft_email`, `full_lifecycle_advance`, `clear_report_cache`, `initiate_call`, `call_contact`, `invite_user`, `instantiate_workflow_template`). Bullet dodged — the bug existed but the affected tools were never invoked in production; recently-added DocuSeal `send_document_for_signing` and other AI-driven tools hadn't been wired into AiSHA chat surface yet at the time of fix. **Integration test added** (5 assertions, all passing): (a) every `TOOL_REGISTRY` entry whose source `.braid` file declares `@policy(...)` has a matching `policy:` field — drift in any of the three layers (`.braid` source → `generate-registry.js` → committed `registry.js`) breaks CI; (b) regression pin: the 10 tools from PR #565 must remain `WRITE_OPERATIONS`; (c) every `.braid` function with `@policy` has a `TOOL_REGISTRY` entry (catches "added function but forgot to register"); (d) every registry policy is one of `{READ_ONLY, WRITE_OPERATIONS}` (the runtime two-bucket schema; `DELETE_OPERATIONS` / `ADMIN_ONLY` / `EXTERNAL_API` from policies.js are valid in `.braid` but coerce to `WRITE_OPERATIONS` for the registry); (e) parser smoke test asserts ≥50 `@policy`-annotated functions loaded (guards against the test silently passing if `TOOLS_DIR` resolution breaks). Test reuses `parseBraidFile` from `braid-llm-kit/tools/generate-registry.js` so the same parser is exercised end-to-end. **Three-layer enforcement model** (codified by this card): `.braid` source declares `@policy` (humans write) → `generate-registry.js inferPolicy()` reads it (unit-test pinned in `registry-policy-inference.test.js`) → committed `registry.js` matches (THIS test pins). Drift at any layer fails CI before merge. **Closes 4VD-38.**
