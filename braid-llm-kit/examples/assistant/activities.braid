// Executive Assistant Tools - Activity & Calendar Management
// Schedule meetings, track tasks, manage calendar

import { Result, Activity, CRMError } from "../../spec/types.braid"

// Error handling: HTTP errors return CRMError with APIError tag and status code.
// The backend interprets status codes to determine error type.

@policy(WRITE_OPERATIONS)
fn createActivity(
  tenant: String,
  subject: String,
  activity_type: String,
  due_date: String,
  due_time: String,
  assigned_to: String,
  entity_type: String,
  entity_id: String,
  body: String
) -> Result<Activity, CRMError> !net {
  let url = "/api/v2/activities";
  // Transform entity_type/entity_id to DB column names: related_to/related_id
  let payload = {
    tenant_id: tenant,
    subject: subject,
    activity_type: activity_type,
    due_date: due_date,
    due_time: due_time,
    assigned_to: assigned_to,
    related_to: entity_type,
    related_id: entity_id,
    body: body,
    status: "scheduled",
    metadata: {}
  };
  
  let response = http.post(url, { body: payload });
  
  return match response {
    Ok{value} => Ok(value.data),
    Err{error} => CRMError.fromHTTP(url, error.status, "create_activity"),
    _ => CRMError.network(url, 500, "unknown")
  };
}

@policy(WRITE_OPERATIONS)
fn updateActivity(
  tenant: String,
  activity_id: String,
  updates: Object
) -> Result<Activity, CRMError> !net {
  let url = "/api/v2/activities/" + activity_id;
  
  let response = http.put(url, { body: updates });
  
  return match response {
    Ok{value} => Ok(value.data),
    Err{error} => CRMError.fromHTTP(url, error.status, "update_activity"),
    _ => CRMError.network(url, 500, "unknown")
  };
}

@policy(WRITE_OPERATIONS)
fn markActivityComplete(
  tenant: String,
  activity_id: String
) -> Result<Activity, CRMError> !net {
  let url = "/api/v2/activities/" + activity_id;
  let updates = { status: "completed" };
  
  let response = http.put(url, { body: updates });
  
  return match response {
    Ok{value} => Ok(value.data),
    Err{error} => CRMError.fromHTTP(url, error.status, "complete_activity"),
    _ => CRMError.network(url, 500, "unknown")
  };
}

@policy(READ_ONLY)
fn getUpcomingActivities(
  tenant: String,
  assigned_to: String,
  days: Number
) -> Result<Array, CRMError> !net {
  let url = "/api/v2/activities";
  // TODO: Filter by due_date range using days param when API supports date range queries
  let params = {
    tenant_id: tenant,
    assigned_to: assigned_to,
    status: "scheduled",
    limit: days * 10
  };
  
  let response = http.get(url, { params: params });
  
  // Note: Empty array is a VALID result (no upcoming activities), not an error
  return match response {
    Ok{value} => Ok(value.data),
    Err{error} => CRMError.fromHTTP(url, error.status, "list_activities"),
    _ => CRMError.network(url, 500, "unknown")
  };
}

@policy(WRITE_OPERATIONS)
fn scheduleMeeting(
  tenant: String,
  subject: String,
  attendees: Array,
  date_time: String,
  duration_minutes: Number,
  assigned_to: String
) -> Result<Activity, CRMError> !net {
  let url = "/api/v2/activities";
  let attendee_list = attendees.join(", ");
  let payload = {
    tenant_id: tenant,
    subject: subject,
    activity_type: "meeting",
    due_date: date_time,
    assigned_to: assigned_to,
    body: "Attendees: " + attendee_list,
    status: "scheduled",
    metadata: { duration_minutes: duration_minutes, attendees: attendees }
  };
  
  let response = http.post(url, { body: payload });
  
  return match response {
    Ok{value} => Ok(value.data),
    Err{error} => CRMError.fromHTTP(url, error.status, "schedule_meeting"),
    _ => CRMError.network(url, 500, "unknown")
  };
}

@policy(WRITE_OPERATIONS)
fn deleteActivity(
  tenant: String,
  activity_id: String
) -> Result<Boolean, CRMError> !net {
  let url = "/api/v2/activities/" + activity_id;
  let response = http.delete(url, {});
  return match response {
    Ok{value} => Ok(true),
    Err{error} => CRMError.fromHTTP(url, error.status, "delete_activity"),
    _ => CRMError.network(url, 500, "unknown")
  };
}

@policy(READ_ONLY)
fn listActivities(
  tenant: String,
  status: String,
  entity_type: String,
  entity_id: String,
  limit: Number
) -> Result<Array, CRMError> !net {
  let url = "/api/v2/activities";
  // entity_type: lead, contact, account, opportunity
  // API query params use related_to_type/related_to_id
  let params = {
    tenant_id: tenant,
    status: status,
    related_to_type: entity_type,
    related_to_id: entity_id,
    limit: limit
  };
  
  let response = http.get(url, { params: params });
  
  // Note: Empty array is a VALID result (no activities match filter), not an error
  return match response {
    Ok{value} => Ok(value.data),
    Err{error} => CRMError.fromHTTP(url, error.status, "list_activities"),
    _ => CRMError.network(url, 500, "unknown")
  };
}

@policy(READ_ONLY)
fn getActivityDetails(
  tenant: String,
  activity_id: String
) -> Result<Activity, CRMError> !net {
  let url = "/api/v2/activities/" + activity_id;
  let params = { tenant_id: tenant };
  
  let response = http.get(url, { params: params });
  
  return match response {
    Ok{value} => Ok(value.data),
    Err{error} => CRMError.fromHTTP(url, error.status, "get_activity"),
    _ => CRMError.network(url, 500, "unknown")
  };
}

@policy(READ_ONLY)
fn searchActivities(
  tenant: String,
  query: String,
  limit: Number
) -> Result<Array, CRMError> !net {
  // Use list endpoint with search param - filters by subject
  let url = "/api/v2/activities";
  let params = {
    tenant_id: tenant,
    search: query,
    limit: limit
  };
  
  let response = http.get(url, { params: params });
  
  // Note: Empty array is a VALID result (no matches found), not an error
  return match response {
    Ok{value} => Ok(value.data.activities),
    Err{error} => CRMError.fromHTTP(url, error.status, "search_activities"),
    _ => CRMError.network(url, 500, "unknown")
  };
}
