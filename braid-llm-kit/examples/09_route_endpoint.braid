@route(method: "GET", path: "/api/time")
fn get_time() -> u64 !clock {
  let t: u64 = 0; // placeholder for actual clock call
  t
}

@route(method: "POST", path: "/api/leads")
fn create_lead(data: LeadInput) -> Result[Lead, Error] !net {
  let lead: Lead = {}; // placeholder
  Ok(lead)
}

type LeadInput = { name: String, email: String }
type Lead = { id: u64, name: String, email: String }
type Error = String
