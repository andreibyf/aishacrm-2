# Frontend env for Docker/Compose
# Copy to .env (root) or pass with `--env-file .env.docker`

# Backend API URL (Docker Compose maps backend to localhost:3001)
VITE_AISHACRM_BACKEND_URL=http://localhost:3001

# Supabase (Frontend)
VITE_SUPABASE_URL=https://ehjlenywplgyiahgxkfj.supabase.co
VITE_SUPABASE_ANON_KEY=REPLACE_WITH_YOUR_SUPABASE_ANON_KEY

# Build/Runtime
NODE_ENV=production
